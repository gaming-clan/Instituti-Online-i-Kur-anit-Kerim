{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile containing personal information and role.",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "fullName": { "type": "string" },
        "roles": { "type": "array", "items": { "type": "string", "enum": ["student", "teacher", "admin", "superadmin"] } },
        "role": { "type": "string", "enum": ["student", "teacher", "admin", "superadmin"] },
        "phone": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["email", "fullName", "role", "createdAt"]
    },
    "Subject": {
      "title": "Subject",
      "description": "A subject taught in the institute.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["title", "description", "createdAt"]
    },
    "Class": {
      "title": "Class",
      "description": "A specific class instance of a subject.",
      "type": "object",
      "properties": {
        "subjectId": { "type": "string" },
        "title": { "type": "string" },
        "teacherId": { "type": "string" },
        "semester": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["subjectId", "title", "teacherId", "createdAt"]
    },
    "Enrollment": {
      "title": "Enrollment",
      "description": "A student enrolled in a class.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "enrolledAt": { "type": "number" },
        "status": { "type": "string", "enum": ["active", "dropped", "completed"] }
      },
      "required": ["studentId", "enrolledAt", "status"]
    },
    "Session": {
      "title": "Session",
      "description": "A class session on a specific date for attendance.",
      "type": "object",
      "properties": {
        "date": { "type": "number" },
        "topic": { "type": "string" }
      },
      "required": ["date"]
    },
    "AttendanceRecord": {
      "title": "Attendance Record",
      "description": "A student's attendance for a specific session.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "status": { "type": "string", "enum": ["present", "absent", "late", "excused"] }
      },
      "required": ["studentId", "status"]
    },
    "Exam": {
      "title": "Exam",
      "description": "An online exam for a class.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "date": { "type": "number" },
        "durationMinutes": { "type": "number" },
        "questions": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "text": { "type": "string" },
              "options": {
                "type": "array",
                "items": { "type": "string" }
              },
              "correctOptionIndex": { "type": "number" }
            }
          }
        }
      },
      "required": ["title", "date", "questions"]
    },
    "ExamSubmission": {
      "title": "Exam Submission",
      "description": "A student's submission for an exam.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "answers": {
          "type": "array",
          "items": { "type": "number" }
        },
        "score": { "type": "number" },
        "submittedAt": { "type": "number" }
      },
      "required": ["studentId", "answers", "submittedAt"]
    },
    "Material": {
      "title": "Material",
      "description": "A class material (file, link, etc).",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "url": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "description", "url"]
    },
    "Post": {
      "title": "Post",
      "description": "An announcement or post for a class.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "content": { "type": "string" },
        "authorName": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "content", "authorName"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user profiles."
    },
    "/subjects/{subjectId}": {
      "schema": { "$ref": "#/entities/Subject" },
      "description": "Stores global subjects."
    },
    "/classes/{classId}": {
      "schema": { "$ref": "#/entities/Class" },
      "description": "Stores class instances."
    },
    "/classes/{classId}/enrollments/{enrollmentId}": {
      "schema": { "$ref": "#/entities/Enrollment" },
      "description": "Stores student enrollments for a class."
    },
    "/classes/{classId}/sessions/{sessionId}": {
      "schema": { "$ref": "#/entities/Session" },
      "description": "Stores sessions and attendance."
    },
    "/classes/{classId}/exams/{examId}": {
      "schema": { "$ref": "#/entities/Exam" },
      "description": "Stores exams and results."
    },
    "/classes/{classId}/materials/{materialId}": {
      "schema": { "$ref": "#/entities/Material" },
      "description": "Stores materials for a class."
    },
    "/classes/{classId}/posts/{postId}": {
      "schema": { "$ref": "#/entities/Post" },
      "description": "Stores posts for a class."
    }
  }
}
